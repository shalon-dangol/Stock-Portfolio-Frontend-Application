// Deprecated re-export: kept for backwards compatibility if imported directly.
// Prefer importing from ./portfolioSlice and ./index
export * from "./portfolioSlice";
export { default } from "./portfolioSlice";
